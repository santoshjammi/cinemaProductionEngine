---
name: "production-design-inputs"
type: "domain_facts"
version: "2.0"
---

# Production Design Inputs (from `cinemaProductiondesignInputs*.md`)

These inputs serve as the source of truth for visual consistency across all AI-generated content in `videoGen`.
